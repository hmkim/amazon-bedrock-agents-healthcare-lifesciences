[
  {
    "name": "fetch_supplementary_info_from_doi",
    "description": "Fetches supplementary information for a paper given its DOI and returns a research log.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "doi": {
          "type": "string",
          "description": "The paper DOI (e.g., '10.1038/nature12373')"
        },
        "output_dir": {
          "type": "string",
          "description": "Directory to save supplementary files (default: 'supplementary_info')"
        }
      },
      "required": ["doi"]
    }
  },
  {
    "name": "query_arxiv",
    "description": "Query arXiv for papers based on the provided search query.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The search query string"
        },
        "max_papers": {
          "type": "integer",
          "description": "The maximum number of papers to retrieve (default: 10)"
        }
      },
      "required": ["query"]
    }
  },
  {
    "name": "query_scholar",
    "description": "Query Google Scholar for papers based on the provided search query.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The search query string"
        }
      },
      "required": ["query"]
    }
  },
  {
    "name": "query_pubmed",
    "description": "Query PubMed for papers based on the provided search query.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The search query string"
        },
        "max_papers": {
          "type": "integer",
          "description": "The maximum number of papers to retrieve (default: 10)"
        },
        "max_retries": {
          "type": "integer",
          "description": "Maximum number of retry attempts with modified queries (default: 3)"
        }
      },
      "required": ["query"]
    }
  },
  {
    "name": "search_google",
    "description": "Search using Google search.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The search query (e.g., 'protocol text or search question')"
        },
        "num_results": {
          "type": "integer",
          "description": "Number of results to return (default: 3)"
        },
        "language": {
          "type": "string",
          "description": "Language code for search results (default: 'en')"
        }
      },
      "required": ["query"]
    }
  },
  {
    "name": "extract_url_content",
    "description": "Extract the text content of a webpage using requests and BeautifulSoup.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "Webpage URL to extract content from"
        }
      },
      "required": ["url"]
    }
  },
  {
    "name": "extract_pdf_content",
    "description": "Extract the text content of a PDF file given its URL.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "URL of the PDF file to extract text from"
        }
      },
      "required": ["url"]
    }
  }
]
